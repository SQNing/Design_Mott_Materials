# Feature Test Report: Design_Mott_Materials

**Date:** 2026-04-21 19:45 CST
**Project type:** CLI / scientific workflow
**Features tested:** Sunny / Julia Phase 1 LSWT environment migration retargeted to Sunny 0.9.0, plus real FeI2 `V2b` downstream smoke
**Profile:** ephemeral
**Use Case:** Verify that the corrected canonical Phase 1 environment can resolve the real upstream `Sunny 0.9.0` release, instantiate cleanly under Julia 1.12.6, and rerun the direct LSWT probe plus the real FeI2 `V2b` downstream chain.
**Expected Outcome:** `lswt` remains `ready`, the LSWT backend no longer fails for environment bootstrapping reasons, and any remaining error is a real Sunny/backend instability under `Sunny 0.9.0`.
**Verdict:** partial
**Critical Issues:** 1

## Summary

| Feature | Discoverable | Setup | Works | Expected Outcome Met | Doc Quality |
|---------|-------------|-------|-------|---------------------|-------------|
| Sunny / Julia Phase 1 LSWT environment migration on Sunny 0.9.0 | yes | yes | partial | yes | repo docs now match the real upstream version line |

## Per-Feature Details

### Sunny / Julia Phase 1 LSWT environment migration on Sunny 0.9.0
- **Role:** Downstream user validating that the canonical LSWT environment now points at the actual upstream Sunny release line rather than the earlier mistaken `0.7.x` cap.
- **Use Case:** Repoint the canonical environment to `.julia-env-v09`, set `Sunny = "0.9"` in the repo-local Julia project, instantiate against the same local depot, then rerun both a direct LSWT probe and the real FeI2 `V2b` smoke with `DESIGN_MOTT_JULIA_CMD=/data/work/zhli/soft/julia-1.12.6/bin/julia`.
- **What they tried:** Updated the launcher/docs/tests to `.julia-env-v09` and `Sunny.jl 0.9.x`, instantiated the new environment, confirmed the resolver selected `Sunny v0.9.0`, reran the focused regression slice, reran the direct LSWT probe, and reran the real FeI2 smoke.
- **Discoverability:** The environment contract is now more coherent because the directory identity (`v09`) matches the intended Sunny line.
- **Setup:** Julia 1.12.6 remained at `/data/work/zhli/soft/julia-1.12.6`, and the new repo-local environment resolved `Sunny v0.9.0` successfully from the local registry plus depot.
- **Functionality:** The focused regression slice stayed green with `32 passed`. The direct LSWT probe still reaches real Sunny execution, now returning a `Sunny.InstabilityError(...)` wrapper in the error message. The real FeI2 rerun kept `lswt=ready`, `gswt=blocked`, and `thermodynamics=review`, and the `lswt` stage now fails with `Sunny.InstabilityError("Not an energy-minimum; wavevector q = [0, 0, 0] unstable.")`.
- **Expected vs Actual Outcome:** Phase 1 remains successful at the environment level. Upgrading from `0.7.8` to `0.9.0` did not remove the narrowed LSWT instability; it only confirmed that the same failure class persists on the corrected upstream release line.
- **Blocked steps:** FeI2 LSWT still needs a backend-stable classical reference or a narrower interpretation of the LSWT input state.
- **Friction points:** None on the environment side after the correction; the remaining issue is now clearly in the runtime/physics layer.
- **Doc suggestions:** Keep the `0.9.x` line in the environment reference and note that Phase 1 solves environment convergence, not LSWT stability for every input state.

## Expected vs Actual Outcome

The corrected `Sunny 0.9.0` Phase 1 environment behaved as intended:

- `.julia-env-v09` instantiated successfully
- `Sunny v0.9.0` resolved and precompiled under Julia 1.12.6
- focused regression remained green with `32 passed`
- both the direct LSWT probe and the real FeI2 smoke reached real Sunny execution

The remaining mismatch is still a genuine backend instability:

- code: `backend-execution-failed`
- message: `Sunny.InstabilityError("Not an energy-minimum; wavevector q = [0, 0, 0] unstable.")`

## Issues Found

- Upgrading to `Sunny 0.9.0` does not eliminate the FeI2 LSWT instability; it confirms that the current classical reference is still not a Sunny energy minimum for the LSWT stage.

## Suggestions

- Treat the `0.9.0` migration as the correct Phase 1 baseline and merge it independently of the FeI2 instability question.
- If we continue immediately, the next targeted task should be a narrow LSWT-stability investigation for the FeI2 classical state rather than more environment work.
